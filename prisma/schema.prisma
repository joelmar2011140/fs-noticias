generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Usuario {
  userId String @id @default(uuid())
  email String @unique
  nome String 
  papel Papel @default(PUBLICADOR)
}

enum Papel {
  PUBLICADOR
  ADMIN
}